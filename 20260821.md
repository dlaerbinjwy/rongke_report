# 周报（2026-08-15 ~ 08-21）

**本周小结**：完成 KDA 算子 L1 尾块逻辑补全并推入 PR，同时首次拿到算子性能基线数据；另完成三份汇报 PPT。

| 任务名称 | 具体工作 | 心得 |
|---|---|---|
| **KDA 算子编写** | 1. **L1 尾块逻辑补全**：放开 `SEQ % C != 0` 限制，六段 kernel 与流水入口全部支持不满的尾块，910B 实测全绿，已推入 PR #1605。<br>2. **性能测试**：用 msprof 完成六段全景采集，拿到首份基线——单次完整 prefill 约 6.0 ms，其中 kkt 占 54%，前三段合计 91%；并定位到瓶颈根因。 | 原以为瓶颈在访存，实测发现搬运只占 5%，标量却占了近一半——真正的问题是指令发射和同步。根因是同一个 `T.Parallel`，带广播的写法被编成 64 条小指令，不带的编成 1 条大指令，差 64 倍。另一个意外是段间空隙只占 1.2%，说明"六个 kernel 未融合"的代价不在下发开销上，而在中间量的 GM 往返。性能问题确实不能靠读代码猜。 |
| **汇报 PPT 制作** | 完成三份 30 页量级 deck：AI 原生通信系统架构、DFSS 拟态防护论文精读、近场 V2I 通信与 KDA 昇腾部署调研；含论文原图重制与排版优化。 | 〔待补〕 |

---

<!-- 以下为附录，交周报时可删 -->

## 附：本周实测数据（备查）

形状 B=1, T=4096, H=HV=4, K=V=128, C=64，板卡 Ascend910_9362。完整记录见 `RUN_LOGS/perf_baseline_20260821.md`。

**六段耗时分布**（全流程 msprof，5 轮取稳态，已剔除含编译的第 1 轮）：

| # | 段 | 耗时(us) | 占比 |
|---|---|---|---|
| 2 | **kkt** | **3237.44** | **54.0%** |
| 6 | chunk_o | 1173.92 | 19.6% |
| 3 | solve_tril | 1065.44 | 17.8% |
| 5 | chunk_h | 349.89 | 5.8% |
| 4 | wy_fast | 129.29 | 2.2% |
| 1 | cumsum | 36.22 | 0.6% |
| | **合计** | **5992.20** | |

**三条要改口的结论**：

1. "kkt 占 70% 工作量"是估的，实测 **54.0%**。
2. 该盯的是**三段不是一段**：kkt + chunk_o + solve_tril = **91.4%**。其中 `solve_tril` 17.8% 属意外——它一直被当作"零改动复用 GDN"的那段。
3. **段间空隙合计仅 74.1 us / 1.2%** ——"六个 kernel 未融合"的代价不在下发开销上。

**交叉验证**：kkt 单算子模式 3235.96 us vs 全流程 3237.44 us，**差 0.05%**；四轮之间波动 <0.05%。

**根因**：`get_kernel_source()` 确认 kkt 是纯 Vector（`ASCEND_IS_AIC` = 0、`Mmad` = 0），
但被声明成 `KERNEL_TYPE_MIX_AIC_1_2`，256 个 Cube 核空转。
源码六个 `T.Parallel` 里，两个纯逐元素的编成 1 条 8192 元素指令，四个带广播的编成 64 条 128 元素指令。
按源码推算每 block `GetValue` 应为 32 × 129 = **4128**，msprof 实测 `UB Read Scalar` = **4128**，逐位吻合。

**命中官方反模式四条**：Vector 内逐行 for loop / 冗余全局同步 / 基础指令拼接未融合 / 任务数高于物理核数。

## 附：下周

1. 按官方流程走 Step 3——读 `optimization-guide.md` §2.6 指令向量化、§2.7 指令融合的约束，产出 `optimization_log.md`，逐项实施 + 每项 msprof 验证。
2. 优化优先级已由数据给出：kkt → chunk_o → solve_tril。
3. varlen / `cu_seqlens`，L1 收官项，进行中。
